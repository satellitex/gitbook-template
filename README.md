# substrate-jp
Substrate 日本語 Wiki

## Previous develop
```
$ npm install gitbook-cli -g
$ gitbook --version
```

## Update Docsments
```
$ gitbook build . docs
$ git commit -m "commit message"
$ git push origin branch_name
```